# Dependencias solo para desarrollo y pruebas. No hacen falta en el servidor.
#   pip install -r requirements-dev.txt
#   python -m pytest
-r requirements.txt
pytest==9.1.1
# Lo necesita el TestClient de FastAPI para probar la API por HTTP.
httpx==0.28.1

# Solo para las pruebas: leer de vuelta el PDF y comprobar que el membrete es el
# del emisor correcto. No entra en requirements.txt porque el sistema genera PDF,
# no los lee.
pypdf==6.16.1
